import { FeedV2BrandLoader } from '@/components/feed/smart/FeedV2BrandLoader'

/** Replaces the beige timeline skeleton while /feed-v2 streams. */
export default function FeedV2Loading() {
  return <FeedV2BrandLoader />
}
